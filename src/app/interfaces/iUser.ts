export interface IUser { //--- ToDo
  username: {
    type: string;
    value: string;
  };
  email: {
    type: string;
    value: string;
  };
  // ToDo: update it to be list
  role: {
    type: string;
    value: string;
  };
}
